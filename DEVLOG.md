# DEVLOG — 「과부하」 개발 기록

이 파일은 공모전 제출용 "Codex와 함께 게임을 만든 과정" 서술의 원본이다.
작업 단위마다 아래 형식으로 추가한다. 기존 항목은 수정하지 않는다.

---

## [2026-08-04 23:43] 프로젝트 기록 체계 부트스트랩
- **마일스톤**: 부트스트랩
- **요청받은 것**: 게임 코드 작성 전에 상시 지침, 개발 기록, 에셋 라이선스 표, 제출물 초안 폴더를 만들고 첫 개발 기록을 남길 것.
- **Codex가 구현한 것**: 프로젝트 상시 규칙을 담은 AGENTS.md, 작업별 누적 기록용 DEVLOG.md, 외부 에셋 권리 추적용 CREDITS.md, 빈 제출 문서 2개와 16:9 썸네일 폴더로 구성된 submission/ 뼈대를 생성했다.
- **사람이 직접 한 것**: 프로젝트의 범위, 기술·아트·사운드 제약, 마일스톤 중단 규칙, 공모전 기록 형식과 제출물 구조를 정의했다.
- **해결한 문제**: 개발 이력과 에셋 권리 정보를 나중에 복원하기 어려운 상태 → 구현 전에 기록 책임과 저장 위치를 고정하고 첫 작업부터 사람/Codex의 기여를 분리해 기록했다.
- **시도했다가 버린 접근**: 없음. 지시서가 요구한 부트스트랩 범위만 그대로 적용했다.
- **남은 이슈**: M0 웹 배포 경로 검증은 아직 시작하지 않았다.
- **변경 파일**: AGENTS.md, DEVLOG.md, CREDITS.md, submission/게임소개_200자.md, submission/codex_과정.md, submission/thumbnail_16x9/

---

## [2026-08-05 00:18] M0 단일 스레드 웹 배포 경로 검증
- **마일스톤**: M0
- **요청받은 것**: 빈 Godot 4 프로젝트에서 화면 클릭 시 짧은 효과음을 즉시 재생하고, 스레드 없이 웹으로 익스포트해 공개 URL을 확보할 것.
- **Codex가 구현한 것**: Godot 4.7.1 프로젝트와 960×540 M0 확인 화면을 만들고, 외부 에셋 없이 런타임에 16비트 모노 PCM 클릭음을 생성해 좌클릭 이벤트에서 즉시 재생하도록 구현했다. Web 프리셋의 thread_support와 extensions_support를 끄고, Godot CI 고정 버전으로 headless 실행 및 release export를 통과시켰다. 소스는 main 브랜치, 웹 빌드는 gh-pages 브랜치로 분리해 https://dlsxj101.github.io/overload/ 에 배포했으며 공개 HTML·JS·PCK·WASM의 HTTP 200 응답과 WASM MIME, GODOT_THREADS_ENABLED=false를 확인했다.
- **사람이 직접 한 것**: Godot 4, 무스레드 웹 빌드, 다른 PC 브라우저 확인, 클릭-소리 지연 판정이라는 M0 기준을 정의했다. 실제 다른 PC에서의 청감 판정은 아직 하지 않았다.
- **해결한 문제**: 반복 익스포트에서 기존 web/ 결과물이 all_resources 필터에 다시 포함될 수 있음 → export 제외 필터에 web/*를 추가하고 깨끗한 디렉터리에서 다시 빌드해 PCK를 5,128바이트로 유지했다. 공개 주소가 처음 404를 반환함 → GitHub Pages 배포 작업의 queued/building 상태를 추적해 완료 후 HTML·필수 자산을 다시 검사했다. Headless 컨테이너에서 시스템 fontconfig 경고 발생 → 프로젝트는 시스템 폰트를 요구하지 않고 Godot 파서·런타임·익스포트가 성공함을 분리해 확인했다.
- **시도했다가 버린 접근**: 인앱 브라우저로 로컬 빌드를 직접 클릭해 화면과 입력을 자동 검증하려 했으나, 브라우저 연결기가 WSL 작업경로를 로컬 URI로 해석하지 못해 중단했다. 대신 Godot headless 실행, 로컬 HTTP 응답, 공개 Pages 자산 및 스레드 플래그 검증으로 대체했으며 청감 검증은 자동 통과 처리하지 않았다.
- **남은 이슈**: 다른 PC의 브라우저에서 공개 URL이 검은 화면 없이 실제 실행되는지, 첫 클릭부터 소리가 나는지, 클릭과 소리 사이 지연이 체감되는지를 사람이 직접 판정해야 한다. 이 판정 전까지 M0 통과는 보류한다.
- **변경 파일**: .gitignore, project.godot, export_presets.cfg, main.tscn, main.gd, main.gd.uid, DEVLOG.md, web 배포 산출물(gh-pages 브랜치)

---

## [2026-08-05 07:19] M0 사람 검증 및 통과 확정
- **마일스톤**: M0
- **요청받은 것**: 다른 PC의 Chrome 또는 Edge에서 공개 빌드의 화면, 첫 클릭 소리, 연속 클릭 반응을 사람이 직접 확인하고 다음 단계 진행 여부를 판정할 것.
- **Codex가 구현한 것**: 사람의 검증 결과를 M0 통과 근거로 기록하고 M1 착수 조건이 충족되었음을 확정했다.
- **사람이 직접 한 것**: 공개 URL에서 화면 정상 표시, 첫 클릭 소리 정상, 연속 클릭 즉각 반응을 직접 확인했으며 체감이 좋고 다음 단계로 진행해도 된다고 판정했다.
- **해결한 문제**: 자동화로 판정할 수 없었던 다른 PC 브라우저 호환성과 실제 청감 지연 여부 → 사람이 Chrome 또는 Edge에서 직접 실행·청감 검증해 이상 없음을 확인했다.
- **시도했다가 버린 접근**: 없음. M0에서 남겨 둔 사람 검증 절차를 그대로 수행했다.
- **남은 이슈**: 없음. M0 통과.
- **변경 파일**: DEVLOG.md

---

## [2026-08-05 07:41] M1 타격감 단일 검증 구현
- **마일스톤**: M1
- **요청받은 것**: 빈 원형 아레나에서 플레이어가 움직이지 않는 더미 1마리를 마우스 방향으로 연타할 수 있게 하고, 히트스톱·속도 대비·순백 플래시·넉백·방향성 화면 흔들림·3레이어 사운드와 실시간 튜닝 도구 및 의도형 프리셋 20개를 구현할 것.
- **Codex가 구현한 것**: 물리 엔진 없이 WASD 이동과 부채꼴 스윕 충돌을 직접 계산하고, 적 1마리를 위치·넉백·플래시 배열로 관리했다. 실제 SceneTree 일시정지와 벽시계를 결합한 0.05초 히트스톱, 정지 중 좌클릭 1회 버퍼링, 0.15초 준비/0.04초 스윙 대비, 3프레임 순백 플래시, 정확한 거리의 ease-out 넉백 실루엣, 타격 방향으로 밀린 뒤 복귀하는 화면 변환, 코드 스파크를 구현했다. blade/body/tail 각 3종의 44.1kHz 모노 OGG를 직접 합성하고 4보이스 풀에서 동시 재생하며 레이어별 피치 ±8%와 변형 순환을 적용했다. F1 패널에 juice.tres 11개 전 파라미터 슬라이더, FPS/히트스톱 실측, 전체 값 복사, 사용자 프리셋 저장·불러오기, A/B 캡처와 F2 전환, 내장 프리셋 20개를 구현했다. 자동 스모크 테스트와 실제 기본값 히트스톱을 거치는 100회 연속 타격 테스트를 오류·누수 없이 통과했고, 960×540 기본 화면과 튜닝 패널을 네이티브 렌더로 확인했으며 무스레드 웹 release export를 완료했다.
- **사람이 직접 한 것**: M0를 직접 통과 판정하고 M1 진행을 승인했으며, 타격감 6요소와 초기 수치, 100회 연속 타격이라는 최종 감각 기준, 프리셋 생성은 Codex가 하고 최종 선택은 사람이 한다는 역할 분담을 정의했다. 20개 프리셋의 블라인드 비교와 M1 통과 감각 판정은 아직 하지 않았다.
- **해결한 문제**: 0.04초 스윙이 낮은 프레임에서 적을 건너뛸 수 있음 → 이전 각도와 현재 각도 사이 부채꼴 전체를 검사했다. 넉백된 활성 더미를 계속 쫓아가야 함 → 배열 기반 피격 실루엣만 밀려나고 활성 더미는 같은 자리에 즉시 재생성되도록 분리했다. 웹 PCK에서 디렉터리 열거 시 remap 이름 때문에 프리셋 목록이 비어질 수 있음 → 검증된 20개 리소스 경로를 명시 목록으로 고정했다. 프리셋의 시간값이 슬라이더 step에서 반올림됨 → 모든 프리셋을 0.005초 단위에 맞추고 UI 동기화 중 원본 재기록을 차단했다. Godot 내장 폰트에서 한글이 깨짐 → OFL 한글 폰트를 도입하고 CREDITS와 라이선스 전문을 즉시 추가했다. 빠른 연타 중 OGG tail이 잘릴 수 있음 → 레이어별 4보이스 풀로 겹침 재생을 허용했다.
- **시도했다가 버린 접근**: 활성 더미 자체를 넉백시키는 첫 구조는 연속 타격 표적이 계속 이동해 M1 검증 목적과 충돌해 폐기했다. PCK 내부 프리셋을 DirAccess로 자동 열거하는 방식은 export remap에 취약해 폐기했다. 내장 기본 폰트는 한글 글리프가 없어 폐기했다. 인앱 브라우저 자동 상호작용은 WSL 경로 URI 연결 문제로 사용할 수 없어 Godot 네이티브 프레임 렌더, headless 테스트, 웹 산출물 검사로 대체했다.
- **남은 이슈**: 사람이 공개 웹 빌드에서 기본값 및 20개 프리셋을 직접 비교하고, 더미를 100번 연속 때렸을 때 계속 때리고 싶은지 판정해야 한다. 이 판정 전까지 M1 통과는 보류하며 M2는 시작하지 않는다.
- **변경 파일**: project.godot, export_presets.cfg, main.tscn, main.gd, tuning/juice_tuning.gd, tuning/juice.tres, tuning/presets/*.tres, systems/combat.gd, systems/enemy_pool.gd, systems/hitstop.gd, systems/camera_shake.gd, systems/audio_hit.gd, player/player.gd, debug/tuning_overlay.gd, audio/hit/*.ogg, fonts/BlackHanSans-Regular.ttf, fonts/OFL-BlackHanSans.txt, CREDITS.md, tests/m1_smoke_test.gd, tests/m1_100_hit_test.gd, tests/m1_overlay_preview.gd, DEVLOG.md

---

## [2026-08-05 07:43] M1 공개 웹 빌드 배포 검증
- **마일스톤**: M1
- **요청받은 것**: M1 구현을 사람이 직접 100회 타격하고 프리셋을 비교할 수 있도록 공개 웹 빌드로 제공할 것.
- **Codex가 구현한 것**: M1 release export를 gh-pages 커밋 2090f29로 배포하고 Pages 작업 성공을 확인했다. 공개 HTML에서 GODOT_THREADS_ENABLED=false, 공개 WASM의 application/wasm MIME과 HTTP 200을 확인했으며, 공개 PCK와 로컬 최종 PCK의 SHA-256이 2f3cd730d169f6909833abd4a138b397b4d275ac7f3eba60410f9b76dac35539로 일치함을 검증했다.
- **사람이 직접 한 것**: 아직 없음. 공개 빌드의 타격감과 프리셋 최종 선택은 다음 사람 검증에서 수행한다.
- **해결한 문제**: 배포 CDN 캐시 때문에 이전 M0 파일을 받을 가능성 → 빌드 커밋을 쿼리에 포함해 새 파일을 받아 로컬 산출물 해시와 직접 대조했다.
- **시도했다가 버린 접근**: 없음.
- **남은 이슈**: 사람이 공개 URL에서 기본값으로 100회 연속 타격하고, 20개 프리셋을 블라인드 비교해 계속 때리고 싶은 느낌과 최종 수치를 판정해야 한다. M1 통과 전 M2 착수 금지.
- **변경 파일**: DEVLOG.md, web 배포 산출물(gh-pages 브랜치)

---

## [2026-08-05 07:48] M1 프리셋 블라인드 비교 보완
- **마일스톤**: M1
- **요청받은 것**: 의도가 다른 프리셋 20개를 사람이 이름에 따른 선입견 없이 블라인드로 비교할 수 있게 할 것.
- **Codex가 구현한 것**: 매 실행 시 20개 프리셋 순서를 무작위로 섞고 드롭다운에는 실제 이름 대신 후보 01~20만 표시하도록 변경했다. 비교가 끝난 뒤 선택 후보의 실제 이름을 확인하는 정답 공개 버튼을 추가했으며, 익명 후보 표시와 20개 리소스 로드를 자동 테스트했다. 네이티브 패널 렌더를 재검사하고 gh-pages 커밋 4312256으로 재배포했으며 공개 PCK 해시가 로컬과 일치함을 확인했다.
- **사람이 직접 한 것**: 아직 없음. 익명 후보를 직접 비교하고 최종 후보를 선택하는 판정이 남아 있다.
- **해결한 문제**: heavy_slow, light_fast 같은 의도형 파일명이 UI에 그대로 노출되어 블라인드 비교가 성립하지 않음 → 파일 경로와 표시명을 분리하고 표시명만 무작위 익명 후보로 제공했다.
- **시도했다가 버린 접근**: 실제 프리셋 이름을 드롭다운에 그대로 노출하는 초기 UI를 폐기했다.
- **남은 이슈**: 사람이 후보 번호만 보고 타격감을 평가한 뒤, 선택을 마친 시점에만 정답 공개를 눌러 최종 프리셋 이름과 수치를 확정해야 한다.
- **변경 파일**: debug/tuning_overlay.gd, tests/m1_smoke_test.gd, DEVLOG.md, web 배포 산출물(gh-pages 브랜치)

---

## [2026-08-05 21:44] M1 기준 타격감 확정 및 DNA 라이브러리 선별
- **마일스톤**: M1
- **요청받은 것**: 블라인드 비교에서 선택한 balanced_default를 실제 게임의 기준 타격감으로 고정하고, 비교용 20개 프리셋을 모두 보존하지 말고 향후 캐릭터 특성으로 분화할 가치가 큰 약 5개만 DNA 라이브러리로 남길 것.
- **Codex가 구현한 것**: balanced_default의 11개 수치가 현재 tuning/juice.tres와 완전히 같음을 확인하고 이 파일을 실제 게임 기준값으로 명시했다. 중앙 기준형과 겹치거나 한 요소만 미세 조정한 비교 프리셋 15개를 제거하고, 경량·속공(light_fast), 중량·강타(heavy_slow), 광역·횡베기(wide_cleave), 정밀·결투(precision), 탄성·밀어내기(elastic) 5개만 DNA로 선별했다. F1 패널도 블라인드 후보 20개 대신 한국어 특성이 드러나는 DNA 5개를 고정 표시하도록 바꾸고 자동 검증 기대값을 갱신했다. M1 스모크 테스트와 기본 히트스톱을 포함한 100회 기술 타격 테스트를 모두 통과했으며, 무스레드 웹 release export를 gh-pages 커밋 7537832로 배포했다. Pages 작업 성공 후 공개 PCK와 로컬 PCK의 SHA-256이 9077192427b20a8150f55507870adc42d0bc2f61254ad06b01cb412828f4b493로 일치함을 확인했다.
- **사람이 직접 한 것**: 20개 블라인드 후보를 비교해 후보 6을 가장 선호한다고 판정하고 정답 공개 결과가 balanced_default임을 전달했다. 또한 모든 후보를 캐릭터로 곧바로 구현하는 대신 특색 있는 약 5개만 DNA로 보존하는 방향을 결정했다.
- **해결한 문제**: 미세 차이까지 모두 보존하면 향후 20개 캐릭터 구현 약속처럼 범위가 부풀고 특성 간 경계가 흐려짐 → 속도, 무게, 범위, 정밀도, 밀어내기라는 서로 다른 플레이 축을 대표하는 5개만 남기고 기준형은 실제 게임 설정과 분리했다. 웹 PCK에는 제거한 프리셋이 포함되지 않고 선별한 5개만 패킹되는 것도 export 로그로 확인했다.
- **시도했다가 버린 접근**: 변경된 패널을 CI 컨테이너의 headless 영상 기록으로 재캡처하려 했으나 더미 렌더러가 텍스처를 제공하지 않아 중단했다. X11 가상 디스플레이 방식도 CI 이미지의 그래픽 의존성 구성과 맞지 않아 중단했다. 패널 크기와 행 구조는 그대로이고 더 짧은 단일 프리셋 행의 표시 문자열만 바뀌었으므로 기존 네이티브 렌더 검증과 자동 UI 구성 검증을 유지했다.
- **남은 이슈**: 사람의 최종 M1 통과 판정은 아직 명시적으로 받지 않았다. balanced_default로 약 100회 연속 타격해 계속 때리고 싶은지와 M1 통과 여부를 확인하기 전까지 M2는 시작하지 않는다. 선별 DNA는 향후 설계 참고자료이며 5개 캐릭터 구현 범위를 확정한 것이 아니다.
- **변경 파일**: tuning/juice.tres, tuning/presets/*.tres, debug/tuning_overlay.gd, tests/m1_smoke_test.gd, DEVLOG.md, web 배포 산출물(gh-pages 브랜치)

---

## [2026-08-05 21:49] M1 사람 검증 및 통과 확정
- **마일스톤**: M1
- **요청받은 것**: balanced_default를 기준 타격감으로 사용한 M1을 최종 통과 처리할 것.
- **Codex가 구현한 것**: 앞서 통과한 M1 스모크 테스트, 100회 기술 타격 테스트, 무스레드 웹 export 및 공개 PCK 일치 검증과 사람의 최종 감각 판정을 함께 M1 완료 근거로 기록했다. M2 착수 조건이 충족되었음을 확정했으며 마일스톤 중단 규칙에 따라 M2는 자동으로 시작하지 않았다.
- **사람이 직접 한 것**: 공개 빌드에서 블라인드 비교로 balanced_default를 최종 기준값으로 선택하고, 연속 타격 체감을 포함한 M1을 최종 통과 판정했다.
- **해결한 문제**: 자동 테스트로 대신할 수 없었던 "계속 때리고 싶은가"라는 최종 감각 기준 → 사람이 실제 플레이 후 명시적으로 M1 통과를 선언해 검증을 완료했다.
- **시도했다가 버린 접근**: 없음. 남겨 둔 사람 검증 절차를 그대로 완료했다.
- **남은 이슈**: 없음. M1 통과. M2는 별도 진행 요청을 받은 뒤에만 착수한다.
- **변경 파일**: DEVLOG.md
