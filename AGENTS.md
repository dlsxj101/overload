# AGENTS.md — 「과부하」 프로젝트 상시 지침

## 프로젝트
Godot 4 탑다운 핵앤슬래시 웹게임. 전 세계 대상 공모전 제출작.
최종 산출물은 브라우저에서 로그인 없이 즉시 플레이되는 웹 빌드.

## 최우선 원칙
타격감이 이 게임의 전부다. 기능 개수, 콘텐츠 분량, 코드 우아함보다
"한 번 베는 느낌"이 항상 우선한다. 둘이 충돌하면 타격감을 택한다.

## 작업 규칙
1. 지시서에 없는 기능을 추가하지 않는다. 추가하고 싶으면 구현하지 말고 보고에 적는다.
2. 각 마일스톤 완료 시 멈추고 보고한다. 다음 단계로 자동 진행하지 않는다.
3. 모든 작업 후 DEVLOG.md에 항목을 추가한다. 이것을 하지 않으면 작업 미완료로 간주한다.
4. 외부 에셋을 도입하면 즉시 CREDITS.md에 라이선스를 기록한다.

## 기술 제약 (변경 금지)
- 물리 엔진 사용 금지. RigidBody2D 금지. 이동·충돌은 직접 구현.
- 적은 개별 노드로 만들지 않는다. 배열로 관리하고 배치 렌더링한다.
  (최종적으로 적 120마리 이상이 동시에 존재)
- 스레드 의존 기능 금지. SharedArrayBuffer / COOP·COEP 헤더 요구를 피한다.
- 목표 60fps 고정. 카메라 고정(스크롤·추적 없음).
- 매직 넘버 금지. 모든 튜닝 수치는 tuning/juice.tres 한 곳에만 존재한다.

## 아트 원칙
- 칼, 궤적, 이펙트, 파티클은 항상 코드로 그린다. 이미지 파일을 쓰지 않는다.
- 팔레트 고정: 플레이어 흰색 / 적 검정 실루엣 + 청록 #00E5D0 발광 / 배경 어두운 회청색
- 피격 순간 적은 순백색으로 덮인다.

## 사운드 원칙
- 타격음은 항상 3레이어(blade / body / tail) 동시 재생.
- body 레이어(60~120Hz 저역)가 없으면 타격감이 성립하지 않는다.
- 재생마다 피치 ±8% 무작위. 레이어당 변형 3~4개 로테이션. OGG, 모노.

## UI
텍스트는 한국어. 산업 스텐실 계열 폰트.

## 커밋
`[M1] 히트스톱 구현` 형식. 마일스톤 태그를 앞에 붙인다.
